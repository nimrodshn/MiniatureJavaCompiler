
public interface Scanner {
	/*
	 * Description: This method gets a series of instructions in the form of raw string and outputs
	 * an array containing the the above instructions broken to lines.
	 * @Param: String a set of instructions
	 */
	public String[] scan(String str);
}
